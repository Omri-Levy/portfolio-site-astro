---
const { children, class: classes, ...rest } = Astro.props;
---

<h1
	class={`
		mb-8
		mt-20
		text-center
		text-5xl
		sm:text-8xl
		 ${classes ?? ``}
		 `}
	{...rest}
>
	<slot />
</h1>
