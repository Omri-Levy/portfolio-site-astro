export enum ETheme {
	INITIAL = `initial`,
	LIGHT = `light`,
	DARK = `dark`,
	SYSTEM = `system`,
	LIGHT_ALT = `light-alt`,
	DARK_ALT = `dark-alt`,
	SYSTEM_ALT = `system-alt`,
}

export enum EResolvedTheme {
	LIGHT = `light`,
	DARK = `dark`,
	SYSTEM = `system`,
}

export enum EMethod {
	GET = `GET`,
	POST = `POST`,
	PUT = `PUT`,
	PATCH = `PATCH`,
	DELETE = `DELETE`,
}

export enum ELocale {
	EN = `en`,
	HE = `he`,
	IW = `iw`,
}
